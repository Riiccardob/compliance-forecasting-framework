# Core runtime dependencies - Hybrid Hypergraph-ATG (gamma-synthetic branch)
# Identical to main branch + tqdm (optional, progress bar for ramp injection)
# Install with: pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
networkx>=3.1
prophet>=1.1
cmdstanpy>=1.1
statsmodels>=0.14
scikit-learn>=1.3
scipy>=1.11
pyyaml>=6.0
dash>=2.14
dash-mantine-components>=0.12
plotly>=5.18

# Optional: progress bar during ramp injection (run_gamma_ramp_injector.py)
# tqdm>=4.66
# Core runtime dependencies — Hybrid Hypergraph-ATG
# Install with: pip install -r requirements.txt

# Data manipulation
pandas>=2.0
numpy>=1.24

# Graph modeling
networkx>=3.1

# Forecasting
prophet>=1.1
cmdstanpy>=1.1       # Prophet backend (Stan sampler)
statsmodels>=0.14

# Anomaly detection / machine learning
scikit-learn>=1.3

# Causal analysis (Pearson, Transfer Entropy)
scipy>=1.11

# Configuration
pyyaml>=6.0

# Dashboard
dash>=2.14
dash-mantine-components>=0.12
plotly>=5.18
